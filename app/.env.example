# Copy to .env.local for local development if you need to point at a backend that isn't
# running on the default http://localhost:4000 (e.g. a deployed Render URL during testing).
VITE_API_URL=http://localhost:4000
