// Moved to @chowka/game-core/paths so the online server can share the exact same constants — see
// that file's own comment. Thin re-export so nothing here has to change its import path.
export { AI_SEAT, AI_NAME } from '@chowka/game-core/paths';
